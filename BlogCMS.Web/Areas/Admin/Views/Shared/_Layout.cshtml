@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/admin")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <header>
        <ul class="nav nav-pills">
            <li>
               @Html.ActionLink("Posts", "Index", "Posts", new { area = "Admin" }, new { @class = "btn btn-default" })
            </li>
            <li>
                @Html.ActionLink("Profile", "Index", "Profile", new { area = "Admin" }, new { @class = "btn btn-default" })
            </li>
            <li>
                @Html.ActionLink("New", "New", "Posts")
            </li>
            <li class="pull-right">
                @Html.ActionLink("Back to website", "Index", "Home", new { area = "" }, new { @class="btn btn-default" })
            </li>
        </ul>
    </header>
    <div>
        @RenderBody()
    </div>
    <footer>
    
    </footer>
</body>
</html>
